export const environment = {
  production: false,
  apiUrl: "http://localhost:3000",
  /** Dev: connect Socket.IO via Angular proxy (same origin as the SPA). */
  socketUrl: "",
  /** LiveKit Cloud WebSocket URL (phase 2 voicerooms). */
  livekitUrl: "wss://hive-k10di6bg.livekit.cloud",
};
